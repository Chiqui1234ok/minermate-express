const   helpers = {},
        Investor = require('../models/Investor');

helpers.registerInvestor = async function (data) {
    try {
        // const Investor = new Investor({
        //     userID: data.userID,
        //     suscribedProject
        // })
    }
    catch (err) {
        
    }
}

module.exports = helpers;